spring:
  datasource:
    url: jdbc:h2:mem:test-db
    initialization-mode: always
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
  output:
    ansi:
      enabled: always
author-service:
  host: ${AUTHOR_SERVICE_HOST:localhost}
  port: ${AUTHOR_SERVICE_PORT:8081}