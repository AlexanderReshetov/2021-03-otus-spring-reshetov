spring:
  datasource:
    url: jdbc:h2:mem:test-db
    initialization-mode: always
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
  output:
    ansi:
      enabled: always